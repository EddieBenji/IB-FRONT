import { LoopForNumberPipe } from './loop-for-number.pipe';

describe('LoopForNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new LoopForNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
